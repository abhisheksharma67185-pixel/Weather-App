# Weather-App
Terminal app using Open-Meteo API. Enter any city name for instant weather: temperature, wind speed/direction. No API key needed. Features geocoding, error handling, emoji formatting, infinite loop. Single file, GitHub-safe, production-ready. Run  python weather.py  → type city → see results → repeat or quit. Global coverage, zero setup.
